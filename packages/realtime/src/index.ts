export const REALTIME_STATUS = "inactive";

export function describeRealtimeStatus(): string {
  return "Scout v1 runs synchronously. The realtime package exists as the future queue and live-update seam.";
}
