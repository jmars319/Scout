import { APP_NAME } from "@scout/config";

export const mobileSurface = {
  appName: APP_NAME,
  active: false,
  note: "Mobile remains future expansion only. Any later iOS or Android surface should be derived from the proven desktop-first Scout workflow rather than defining the product."
};
