{
  "name": "@scout/desktopapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node ./scripts/dev.mjs",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "pnpm run build"
  },
  "dependencies": {
    "@scout/config": "workspace:*"
  }
}
